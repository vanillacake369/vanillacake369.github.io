---
title: "[객체지향연습] 영화 예매 시스템 구축"
description: "- 사용자가 온라인 영화 예매 시스템을 활용해서 영화를 예매할 수 있다. - 할인조건에 맞는 예매자는 요금을 할인받을 수 있다."
date: 2024-05-12
tags: [system-design]
lang: ko
draft: false
---

# 요구사항

- 사용자가 온라인 영화 예매 시스템을 활용해서 영화를 예매할 수 있다.
- 할인조건에 맞는 예매자는 요금을 할인받을 수 있다.
- 할인조건은 순서조건과, 기간조건 두가지가 있다.
- 기간조건은 요일 시작시간 종료시간 세부분으로 구성되며 영화 시작시간이 해당 기간안에 포함될 경우 요금을 할인한다.
- 할인 조건에 해당하는 예매자는 할인정책에 따라 요금을 할인받을 수 있다.
- 할인정책은 금액할인정책과 비율할인정책이 있다.
- 영화별로 하나의 할인정책만 할당할 수 있다.

할인정책을 지정하지 않는것도 가능하다.
- 예매를 완료하면 다음과 같은 데이터가 예매자에게 전달되어야한다.

| 정보 | 내용 |
| --- | --- |
| 제목 | 아바타 |
| 상영정보 | 2019년 12월 26일(목) 7회 6:00(오후)~8:00(오후) |
| 인원 | 2명 |
| 정가 | 20,000원 |
| 결제금액 | 18,400원 |

[https://leemoono.tistory.com/18](https://leemoono.tistory.com/18)

# 설계 주의사항

아래를 최대한 의식하고 자각하여 설계하려고 접근했다.

1.

행위 중심 설계 >>> 상태,데이터 중심 설계
2. **역할 단위**로 쪼개기
3.

최대한 **하나의 책임 **수행을 하도록 한다.
4. **도메인 개념(VO)**을 지정하여 확장성 높이기

# 어떤 부분이 확장 가능한가?

- 할인 정책 , 할인조건

# 고민

> 어떻게 할인 조건에 따라 할인 정책들을 적용할 것인가??

```java
public abstract class DiscountPolicyFactory {
    public BigDecimal calculateDiscountAmount(BigDecimal price, CardType cardType) {
        DiscountPolicy discountPolicy = createDiscountPolicy(cardType);
        return discountPolicy.calculateDiscountAmount(price);
    }

    protected abstract DiscountPolicy createDiscountPolicy(CardType cardType);
}
```
```java
public class CardDiscountPolicyFactory extends DiscountPolicyFactory {
    @Override
    protected DiscountPolicy createDiscountPolicy(CardType cardType) {
        return switch (cardType) {
            case 국민 -> new KookminCardDiscountPolicy(new BigDecimal(String.valueOf(0.1)), new BigDecimal(String.valueOf(1000)));
            case 신한 -> new ShinhanCardDiscountPolicy(new BigDecimal(String.valueOf(0.05)), new BigDecimal(String.valueOf(500)));
            case 삼성 -> new SamsungCardDiscountPolicy(new BigDecimal(String.valueOf(0.07)), new BigDecimal(String.valueOf(700)));
        };
    }
}
```

# 다이어그램

![](/images/notion/0c99bff97af2a8e4.png)

# 선웅님의 설계


### 다이어그램

![](/images/notion/81a6b85088791a22.png)

### 코드

`할인정책 :: DiscountPolicy`

- 할인조건들을 은닉화하여 리스트로 저장
- 추상객체를 활용

```java
public abstract class DiscountPolicy {
    protected final List<DiscountCondition> discountConditions = new ArrayList<>();

    public void addDiscountCondition(DiscountCondition discountCondition) {
        this.discountConditions.add(discountCondition);
    }

	abstract int calculateDiscount(Screen screen);
}
```

`할인조건 :: DiscountCondition`

```java
public interface DiscountCondition {
	boolean checkDiscount(Screen screen);
}
```

`금액할인조건 :: AmountDiscountPolicy `

```java
public class AmountDiscountPolicy extends DiscountPolicy{
    private Money amount;

    private AmountDiscountPolicy(Money amount) {
        this.amount = amount;
    }

    public static AmountDiscountPolicy from(Integer amount) {
        return new AmountDiscountPolicy(Money.from(amount));
    }

    @Override
    int calculateDiscount(Screen screen) {
        int regularPrice = screen.getMovie().getPrice();
        int price = regularPrice;
        for (DiscountCondition discountCondition : discountConditions) {
            if(discountCondition.checkDiscount(screen)) {
                price = Math.min(price, regularPrice - amount.getMoney());
            }
        }
        return price;
    }
}
```

[^1]: https://medium.com/29cm/값-객체-value-object-를-활용하여-변경-용이성-개선하기-7457787b84c0 <https://medium.com/29cm/값-객체-value-object-를-활용하여-변경-용이성-개선하기-7457787b84c0>
[^2]: https://inpa.tistory.com/entry/GOF-💠-Enum-Factory-Method-변형-패턴 <https://inpa.tistory.com/entry/GOF-💠-Enum-Factory-Method-변형-패턴>
[^3]: https://velog.io/@qwe916/팩토리-메서드-패턴-Factory-Method-Pattern <https://velog.io/@qwe916/팩토리-메서드-패턴-Factory-Method-Pattern>
[^4]: https://www.youtube.com/watch?v=26S4VFUWlJM&t=2176s&ab_channel=WorkerKSUG <https://www.youtube.com/watch?v=26S4VFUWlJM&t=2176s&ab_channel=WorkerKSUG>
