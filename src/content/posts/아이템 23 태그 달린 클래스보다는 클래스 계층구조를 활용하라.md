---
title: "아이템 23. 태그 달린 클래스보다는 클래스 계층구조를 활용하라"
description: "- 태그 달린 클래스가 **무엇인지**를 배움 - 태그 달린 클래스를 **“지양” 해야하는 이유**를 배움"
date: 2026-02-25
tags: [java]
lang: ko
draft: false
series: { id: "Effective Java", order: 2 }
---

# Why?

왜 배움?

---

---

- 태그 달린 클래스가 **무엇인지**를 배움
- 태그 달린 클래스를 **“지양” 해야하는 이유**를 배움
- 태그 달린 클래스를 **개선하는 방법**을 배움

# What?

그래서 뭐를 사용함?

---

## 태그 달린 클래스란??

- 클래스 내부에 태그값을 위한 타입을 선언
- 태그값을 위한 생성자들을 선언
- 서비스 메서드 내부에, 다형성을 위한 switch(혹은 if-else) 활용

![](/images/notion/ee408009469b9c77.jpeg)

```java
// Tagged class - vastly inferior to a class hierarchy!
class Figure {

	enum Shape { RECTANGLE, CIRCLE };

	// Tag field - the shape of this figure
	final Shape shape;

	// These fields are used only if shape is RECTANGLE
	double length;
	double width;
	// This field is used only if shape is CIRCLE
	double radius;

	// Constructor for circle
	Figure(double radius) {
		shape = Shape.CIRCLE;
		this.radius = radius;
	}

	// Constructor for rectangle
	Figure(double length, double width) {
		shape = Shape.RECTANGLE;
		this.length = length;
		this.width = width;
	}

	double area() {
		switch(shape) {
			case RECTANGLE:
			return length * width;

			case CIRCLE:
			return Math.PI * (radius * radius);

			default:
			throw new AssertionError(shape);
		}
	}
}
```

## 태그 달린 클래스의 단점

- 여러 구현이 한 클래스에 혼합
- 필드를 final로 선언하기 어려움
- 확장을 하기 위해서는 다형성을 위한 switch문을 확장해야함

## 태그 달린 클래스 개선하기 :: 클래스 계층구조 사용

- 하위 클래스에서 **공통적으로 사용되는 필드와 메서드들을 상위 클래스에 올려버리기**
- 태그에 따라 동작이 달라지며, **모든 하위 클래스에서 사용되는 메소드**들은, **루트 클래스의 추상메서드로 선언**
- 확장한 클래스를 체계적으로 구체화하여 정의

<details>
<summary>Effective java 코드</summary>

```java
// Class hierarchy replacement for a tagged class
abstract class Figure {
	abstract double area();
}

class Circle extends Figure {
	final double radius;

	Circle(double radius) { this.radius = radius; }

	@Override double area() { return Math.PI * (radius * radius); }
}

class Rectangle extends Figure {
	final double length;
	final double width;

	Rectangle(double length, double width) {
		this.length = length;
		this.width = width;
	}

	@Override double area() { return length * width; }
}
```
</details>
<details>
<summary>Head First Java 3E Chapter 7 :: inheritance and polymorphism</summary>

![](/images/notion/76fac858df6162db.png)
</details>

## 클래스 계층구조의 장점

- 타입이 의미 별로 따로 존재하니 각 구현체 내부의 변수의 의미를 명시하거나 제한할 수 있고
- 특정 의미만 매개 변수로 받을 수 있다. 
- 타입 사이의 자연스러운 계층 관계를 반영할 수 있어서 `유연성`은 물론 `컴파일 타임` 타입 검사 능력을 높여줄 수 있다.
