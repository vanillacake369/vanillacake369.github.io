---
description: "Object ↔ Byte Stream. 직렬화를 지양해야 하는 이유와 JPA에서 Serializable이 필요한 세 가지 케이스를 정리한다."
tags: [java]
lang: ko
draft: false
---

# Why?

> 왜 그토록 저자가 직렬화 구현을 주의하라고 하는지, 또 JPA를 사용할 때 어떤 경우 직렬화를 처리해주어야하는지 알아보자.

# What?

### 직렬화란?

Object ↔ Byte Stream

> **Object serialization is the process of converting Java objects into byte streams**.

\*\*Usage

- write POJO to,,,

### Serializable을 구현할지는 신중히 결정하라

> 하나의 특징으로 인해 아래 네 가지 이슈가 발현된다.

그 특징은 바로바로,,,

- 수정 확장의 어려움

초기 버전에서 A형태의 직렬화를 제공하는 API를 구성하게 되었다 하자. 다음 API 배포 시, A` 형태로 클래스가 수정확장되게 되었다. A 형태의 직렬화를 의존하던 이전 API 사용자들은 A`형태의 직렬화를 사용할 수가 없다.
( 왜냐하면 A형태의 직렬화는 A형태의 기본 생성자를 의존하는 것이나 마찬가지이기 때문이다)
**따라서 초기에 A형태 직렬화를 제공하기로 하였다면, 무조건 계속 A형태의 직렬화를 제공할 수 밖에 없다.**

- 버그와 보안구멍

역직렬화는 byte stream을 제공하게 된다. 즉, 해당 인스턴스에 대한 기본 생성자를 제공하게 된다. 이를 통해 **인스턴스를 조작할 수 있는 기회를 제공할 수 있다.**[^1]

- 버전 릴리스 별 직렬화를 테스팅 해야함

우리는 항상 버저닝을 하면서 의존성을 확장하게 된다. 초기에는 간단한 시스템이, 자라면 자랄수록 복잡한 요구사항으로 인해 의존성이 추가되기 때문이다. 직렬화는 유연한 설계를 방해한다. 이전 API 버전에서 제공한 직렬화 메서드가, 다음 API 버전에서도 동일하게 적용되어야 하기 때문이다.
**따라서 배포할 때마다 이전 버전들의 모든 직렬화 메서드를 다 테스팅 해야만한다.**

- 상속용으로 설계되었다면 확장 시 재정의 오버헤드가 발생

상속용 클래스라면 지정한 직렬화메서드가 **잘 돌아가는지** 확인해야한다. 재정의하게 된다면 재정의한 직렬화가 **다음 상속에 영향을 끼치지 않는지** 확인해야한다.
상속용 인터페이스라면 이를 implements하는 클래스가 **모두 동일한 직렬화 메서드를 사용하게 되므로 유지보수가 어려워진다.**

### JPA에서 Serializable을 구현해야할 때 : 복합키 사용

> Each entity class consists of persistent fields and properties.

The specification requires that fields of an entity may be Java primitives, Java serializable types, or user-defined serializable types.[^2]

```java
@Entity
public class User {
    @EmbeddedId UserId userId;
    String email;

    // constructors, getters and setters
}

@Embeddable
public class UserId implements Serializable{
    private String name;
    private String lastName;

    // getters and setters
}
```

### JPA에서 Serializable을 구현해야할 때 : id가 아닌 값을 **_JoinColumn_** 으로서 사용

> [Hibernate official documentation](https://hibernate.org/orm/documentation/), when describing mapping in Hibernate, notes that the** referenced field must be serializable when we use \*\*\***referencedColumnName **\***from the **[\***@JoinColumn \*\*\*](https://www.baeldung.com/jpa-join-column)annotation.

Usually, this field is a primary key in another entity. In rare cases of complex entity classes, our reference must be serializable.[^3]

```java
@Entity
public class User {
    @EmbeddedId private UserId userId;
    private Email email;
}

@Entity
public class Email implements Serializable {
    @Id
    private long id;
    private String name;
    private String domain;
}

@Entity
public class Account {
    @Id
    private long id;
    private String type;
    @ManyToOne
    @JoinColumn(referencedColumnName = "email")
    private User user;
}
```

### JPA에서 Serializable을 구현해야할 때 : 엔티티를 바로 **Presentation Layer에 반환**

> When sending objects over the wire using HTTP, we usually create specific DTOs (data transfer objects) for this purpose.

By creating DTOs, we decouple internal domain objects from external services. **If we want to expose our entities directly to the presentation layer without DTOs, then entities must be serializable**.[^4]

### POJO ↔ JSON 직렬화 시, 내부 필드들이 Serializable을 지원하는지 주의하자!

POJO ↔ JSON 직렬화 시, Serializable을 지원하는지 주의해야한다. JPA에서 JSON으로 직렬화/역직렬화를 할 때, 보통 Jackson을 통해 이를 지원하게 된다. 따라서 POJO 내부의 필드들은 모두 직렬화 가능해야만, 이를 key-value형태의 String으로 매핑시켜줄 수 있다.

만약 아래와 같은 Person 엔티티가 있다고 해보자.

```java
public class Person {
    private final String firstName;
    private final String lastName;
    private final int age;

    public Person(String firstName, String lastName, int age) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.age = age;
    }

    public String getFirstName() {
        return firstName;
    }

    public String getLastName() {
        return lastName;
    }

    public int getAge() {
        return age;
    }
}
```

Jackson을 통해 JSON으로 매핑시켜줄 때 아래와 같은 로직을 처리하게 된다. 따라서, 웬만하면 DTO를 사용하여 매핑시켜주도록 하자~!~!

```java
var grant = new Person("Grant", "Hughes", 19);

var mapper = new ObjectMapper();
try {
    var json = mapper.writeValueAsString(grant);
    System.out.println(json);
} catch (JsonProcessingException e) {
    e.printStackTrace();
}
```

> **DTO를 사용해야 하는 이유.**[^5]

# Reference 📚

[https://github.com/clxering/Effective-Java-3rd-edition-Chinese-English-bilingual/blob/dev/Chapter-12/Chapter-12-Item-86-Implement-Serializable-with-great-caution.md](https://github.com/clxering/Effective-Java-3rd-edition-Chinese-English-bilingual/blob/dev/Chapter-12/Chapter-12-Item-86-Implement-Serializable-with-great-caution.md)
[https://www.baeldung.com/jpa-entities-serializable](https://www.baeldung.com/jpa-entities-serializable)
[https://vanillacake369.oopy.io/71d3553d-d75f-40be-8408-84374602e14c](https://vanillacake369.oopy.io/71d3553d-d75f-40be-8408-84374602e14c)
[https://blogs.oracle.com/javamagazine/post/java-json-serialization-jackson](https://blogs.oracle.com/javamagazine/post/java-json-serialization-jackson)

[^1]: 역직렬화 공격(Deserialization Attack)은 OWASP Top 10에 포함된 보안 취약점 중 하나다. 신뢰할 수 없는 데이터를 역직렬화할 경우 원격 코드 실행(RCE)으로 이어질 수 있다.
[^2]: JPA 명세(JSR-338)는 `@EmbeddedId`로 사용하는 복합키 클래스가 `Serializable`을 구현하도록 요구한다. 이를 어기면 런타임에 `InvalidObjectException`이 발생할 수 있다.
[^3]: `@JoinColumn(referencedColumnName = ...)`으로 PK가 아닌 컬럼을 참조할 경우, Hibernate는 해당 필드가 `Serializable`임을 요구한다. 이는 2차 캐시와 세션 간 엔티티 식별에 직렬화가 사용되기 때문이다.
[^4]: Spring MVC에서 `HttpSession`에 엔티티를 저장하거나, RMI/EJB 원격 호출로 엔티티를 전달할 때도 `Serializable` 구현이 필요하다.
[^5]: Jackson은 `Serializable` 인터페이스 자체가 아니라 getter/setter를 기반으로 직렬화하므로, POJO가 `Serializable`을 구현하지 않아도 JSON 변환 자체는 가능하다. 그러나 세션 저장, 캐시 분산 등 Java 직렬화가 개입하는 경로에서는 별도로 구현해야 한다.
